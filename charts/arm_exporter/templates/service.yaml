apiVersion: v1
kind: Service
metadata:
  name: {{ include "arm_exporter.fullname" . }}
  labels:
    {{- include "arm_exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arm_exporter.selectorLabels" . | nindent 4 }}
