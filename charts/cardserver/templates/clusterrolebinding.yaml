apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: sba-discovery-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: sba-discovery-role
subjects:
  - kind: ServiceAccount
    name: "default"
    namespace: "cardserver"
