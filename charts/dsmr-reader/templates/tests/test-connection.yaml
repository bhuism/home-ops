apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dsmr-reader.fullname" . }}-test-connection"
  labels:
    {{- include "dsmr-reader.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dsmr-reader.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
