apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: arm_exporter
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: arm_exporter
  project: default
  source:
    repoURL: https://github.com/bhuism/impl-argocd.git
    path: charts/arm_exporter
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
