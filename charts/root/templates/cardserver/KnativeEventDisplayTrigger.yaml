apiVersion: eventing.knative.dev/v1
kind: Trigger
metadata:
  name: broker-debug-trigger
  namespace: cardserver
spec:
  broker: default
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1
      kind: Service
      name: event-display
