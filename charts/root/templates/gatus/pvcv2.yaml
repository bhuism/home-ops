apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gatus-v2-pvc
  namespace: gatus
spec:
  volumeMode: Filesystem
  resources:
    requests:
      storage: 200Mi
  accessModes:
    - ReadWriteOnce
  storageClassName: impl-default
