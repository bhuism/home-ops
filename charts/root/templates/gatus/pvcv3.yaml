apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gatus-v3-pvc
  namespace: gatus
spec:
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
