apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: home-assistant-v3-pvc
  namespace: home-assistant
spec:
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
