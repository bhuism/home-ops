apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: influx-pvc
  namespace: influx
spec:
  volumeMode: Filesystem
  resources:
    requests:
      storage: 10Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: impl-default
  # volumeName: pvc-ab71d99a-71ee-4f01-b967-5eaff2cfb64e

