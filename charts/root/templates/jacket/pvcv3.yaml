apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jacket-config
  namespace: jacket
spec:
  volumeMode: Filesystem
  resources:
    requests:
      storage: 16Mi
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd
