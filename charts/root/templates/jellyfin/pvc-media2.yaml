apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellyfin-media-pvc2
  namespace: jellyfin
spec:
  volumeMode: 
  resources:
    requests:
      storage: 2300Gi
  accessModes:
    - ReadWriteMany
  volumeMode: Block
  storageClassName: ceph-rbd-twice
