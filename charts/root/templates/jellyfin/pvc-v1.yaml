apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellyfin-v1-pvc
  namespace: jellyfin
spec:
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2200Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd-twice
