apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mosquitto-data-v2-pvc
  namespace: mosquitto
spec:
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
