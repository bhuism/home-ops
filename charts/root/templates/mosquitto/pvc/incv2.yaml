apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mosquitto-inc-v2-pvc
  namespace: mosquitto
spec:
  volumeMode: Filesystem
  resources:
    requests:
      storage: 100Mi
  accessModes:
    - ReadWriteOnce
  storageClassName: impl-default
