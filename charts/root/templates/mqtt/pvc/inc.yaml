apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mosquitto-inc-pvc
  namespace: mosquitto
spec:
  volumeMode: Filesystem
  resources:
    requests:
      storage: 10Mi
  accessModes:
    - ReadWriteOnce
  storageClassName: impl-default
