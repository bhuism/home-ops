apiVersion: v1
kind: Service
metadata:
  name: release-name-mosquitto
  namespace: mosquitto
spec:
  type: ClusterIP
  ports:
    - name: main
      port: 1883
      protocol: TCP
      targetPort: 1883
  selector:
    app.kubernetes.io/name: mosquitto
