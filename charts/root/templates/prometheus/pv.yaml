apiVersion: v1
kind: PersistentVolume
metadata:
  name: prometheus-pv
  namespace: prometheus
  labels:
    app: prometheus
spec:
  storageClassName: local-path
  claimRef:
    name: prometheus-pvc
    namespace: prometheus