apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-v2
  namespace: prometheus
spec:
  volumeMode: Filesystem
  resources:
    requests:
      storage: 15Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
