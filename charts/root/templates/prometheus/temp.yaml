apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: temp
  namespace: prometheus
spec:
  volumeMode: Filesystem
  resources:
    requests:
      storage: 15Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: impl-default
