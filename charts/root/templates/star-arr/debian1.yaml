apiVersion: v1
kind: Pod
metadata:
  name: debian-1
  namespace: starr-arr
spec:
  containers:
    - name: debian-1
      image: debian:stable-slim
      command: ["/bin/sh", "-c"]
      args: ["sleep infinity"]      
