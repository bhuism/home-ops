apiVersion: v1
kind: Pod
metadata:
  name: debian-1
  namespace: star-arr
spec:
  containers:
    - name: debian-1
      image: debian:stable-slim
      command: ["/bin/sh", "-c"]
      args: ["sleep infinity"]      
