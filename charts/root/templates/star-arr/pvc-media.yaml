apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-pvc
  namespace: starr-arr
spec:
  volumeMode: 
  resources:
    requests:
      storage: 2400Gi
  accessModes:
    - ReadWriteMany
  volumeMode: Block
  storageClassName: ceph-rbd-twice
